name: Playit RDP Tunnel

on:
  workflow_dispatch:

jobs:
  setup-rdp-tunnel:
    runs-on: windows-latest

    steps:
    - name: Check out the repository
      uses: actions/checkout@v2

    - name: Download and Install Playit
      run: |
        Invoke-WebRequest -Uri "https://github.com/playit-cloud/playit-agent/releases/download/v0.15.26/playit-windows-x86_64-signed.exe" -OutFile "$env:USERPROFILE\playit.exe"
        Start-Sleep -Seconds 5

    - name: Enable TS
      run: Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server'-name "fDenyTSConnections" -Value 0
    - run: Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
    - run: Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -name "UserAuthentication" -Value 1
    - run: Set-LocalUser -Name "runneradmin" -Password (ConvertTo-SecureString -AsPlainText "p@ssw0rd!" -Force)

    - name: Download and Install Files
      run: |
        # Updated MuMu Link
        Invoke-WebRequest -Uri "https://download1321.mediafire.com/u3ljtir2mf5esv1/y7ry1f29orr/MuMuInstaller_3.1.7.0_gw-overseas12_all_1712735105.exe" -OutFile "$env:USERPROFILE\Downloads\MuMuInstaller.exe"
        Start-Process -FilePath "$env:USERPROFILE\Downloads\MuMuInstaller.exe" -ArgumentList "/S" -Wait
        
        # Updated MAIN_VODKA Link
        Invoke-WebRequest -Uri "https://download1588.mediafire.com/sn3meb58r4e1u2o/oovrsiia3k5/MAIN_VODKA.mumudata" -OutFile "$env:USERPROFILE\Downloads\MAIN_VODKA.mumudata"

    - name: Start Playit and Set Up RDP Tunnel
      env:
        PLAYIT_AUTH_KEY: ${{ secrets.PL }} 
      run: |
        Start-Process -FilePath "$env:USERPROFILE\playit.exe" -ArgumentList "--secret $env:PLAYIT_AUTH_KEY" -NoNewWindow -Wait
        Start-Process -FilePath "$env:USERPROFILE\playit.exe" -NoNewWindow

    - name: Keep the GitHub Action Runner Alive
      run: |
          Start-Sleep -Seconds 11800
